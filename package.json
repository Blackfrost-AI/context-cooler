{
  "name": "context-cooler",
  "version": "6.2.0",
  "description": "Context Cooler — eliminate token burn with the coolest MCP on the net. Sandboxed execution in 11 languages, exit classification, FTS5 search, session continuity, multi-messenger delivery, and 7 platform adapters (Claude Code, Cursor, Codex, Gemini, OpenCode, Grok CLI, Pretzel Porter).",
  "main": "dist/server.js",
  "bin": {
    "context-cooler": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "test": "node --test"
  },
  "keywords": [
    "mcp",
    "context-window",
    "ai-agent",
    "token-optimization",
    "claude",
    "cursor",
    "fts5"
  ],
  "author": "tlancas25",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Blackfrost-AI/context-cooler.git"
  },
  "comment_deps": "CC-S7-010: versions PINNED EXACT (no caret). Install with `npm ci` to honor the lockfile + verify integrity hashes. NOTE: @modelcontextprotocol/sdk transitively pulls a full express/hono/cors/ajv/jose HTTP+OAuth stack (~130 transitive pkgs) that this stdio server does not use; treat it as attack surface and advisory-scan it in CI. better-sqlite3 runs a native build (install script) on install.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.28.0",
    "better-sqlite3": "12.11.1",
    "turndown": "7.2.2",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "@types/turndown": "^5.0.0",
    "typescript": "^5.5.0"
  },
  "bugs": {
    "url": "https://github.com/Blackfrost-AI/context-cooler/issues"
  },
  "homepage": "https://github.com/Blackfrost-AI/context-cooler#readme"
}
